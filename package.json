{
    "name": "my-input-select-library",
    "version": "1.0.10",
    "description": "A custom input select component for React.",
    "main": "dist/index.js",
    "module": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "clean": "if exist dist rmdir /s /q dist",
        "build": "npm run clean && babel src --out-dir dist --extensions \".jsx,.js\" --copy-files",
        "prepublishOnly": "npm run build"
    },
    "keywords": [
        "react",
        "input",
        "select",
        "dropdown"
    ],
    "author": "Batman",
    "license": "MIT",
    "peerDependencies": {
        "react": ">=16.0.0",
        "react-dom": ">=16.0.0"
    },
    "devDependencies": {
        "@babel/cli": "^7.15.7",
        "@babel/core": "^7.26.10",
        "@babel/preset-env": "^7.15.6",
        "@babel/preset-react": "^7.26.3",
        "babel-loader": "^10.0.0"
    },
    "files": [
        "dist"
    ]
}
